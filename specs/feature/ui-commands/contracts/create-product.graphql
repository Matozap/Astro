mutation CreateProduct($command: CreateProductCommand!) {
  createProduct(command: $command) {
    id
    sku
    name
    description
    price {
      amount
      currency
    }
    stockQuantity
    lowStockThreshold
    isActive
    isLowStock
    createdAt
    updatedAt
    createdBy
  }
}
